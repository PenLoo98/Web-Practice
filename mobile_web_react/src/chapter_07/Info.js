// import React {useState} from "react";

function Info(initialValue) {

    const name_input = ""
    const nick_input = ""
    
    // const increaseCount = () => setCount((count) => count+1 )
    // const decreaseCount = () => setCount((count) => Math.max(count - 1, 0));

    return [name_input, nick_input];
}

export default Info;